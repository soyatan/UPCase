const Search = require('./search.svg') as string;
//import Search from './search.svg'
const Plus = require('./plus.svg') as string;
const Edit = require('./edit.svg') as string;
const Trash = require('./trash.svg') as string;
const Back = require('./back.svg') as string;
const Info = require('./info.svg') as string;
const Cancel = require('./cancel.svg') as string;

export const SVGS: any = {
  Search,
  Cancel,
  Back,
  Edit,
  Plus,
  Trash,
  Info,
};
